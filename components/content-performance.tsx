"use client";
import Link from "next/link";
import { useAnalytics } from "@/components/analytics-provider";
import { getSummary } from "@/lib/analytics/calculations";
export default function ContentPerformance({ contentId }: { contentId: string }) { const { records } = useAnalytics(); const summary = getSummary(records.filter(record => record.contentId === contentId)); return <section className="mt-6 rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><div className="flex items-center justify-between"><h2 className="font-bold">Performance</h2><Link href="/dashboard/analytics" className="text-xs font-bold text-[var(--purple-light)]">View analytics →</Link></div>{records.some(record => record.contentId === contentId) ? <div className="mt-5 grid grid-cols-2 gap-4 sm:grid-cols-4">{[["Views", summary.totalViews], ["Engagement", summary.totalLikes + summary.totalComments + summary.totalShares + summary.totalSaves], ["Followers", summary.followersGained], ["Rate", `${summary.engagementRate.toFixed(1)}%`]].map(([label, value]) => <div key={label}><p className="text-xs text-[var(--muted)]">{label}</p><p className="mt-1 text-xl font-bold">{value}</p></div>)}</div> : <p className="mt-4 text-sm text-[var(--muted)]">No performance data yet. <Link href="/dashboard/analytics" className="font-bold text-[var(--purple-light)]">Add performance →</Link></p>}</section> }