"use client";
import Link from "next/link";
import { useState } from "react";
import { useCreator } from "@/components/creator-provider";
import LogoutButton from "@/components/logout-button";

const defaults = { productUpdates: true, contentReminders: true, scheduledContentReminders: true, weeklyGrowthSummary: true, aiRecommendations: true };
export default function SettingsPage() { const { profile, saveProfile } = useCreator(); const [tone, setTone] = useState<string>(profile.contentStyles[0] || "Casual"); const [goal, setGoal] = useState(profile.primaryGoal || ""); const [notifications, setNotifications] = useState(profile.notificationPreferences || defaults); const [saved, setSaved] = useState(false); const save = () => { saveProfile({ contentStyles: [tone as typeof profile.contentStyles[number]], primaryGoal: goal as typeof profile.primaryGoal, notificationPreferences: notifications }); setSaved(true); }; return <main className="min-h-screen bg-[var(--background)] px-5 py-8 sm:px-10"><div className="mx-auto max-w-3xl"><Link href="/dashboard" className="text-sm text-[var(--purple-light)]">← Dashboard</Link><h1 className="mt-10 text-3xl font-bold">Settings</h1><p className="mt-2 text-[var(--muted)]">Keep your creator context and workspace preferences current.</p><section className="mt-8 space-y-5"><div className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><h2 className="text-lg font-bold">Profile</h2><p className="mt-2 text-sm text-[var(--muted)]">{profile.displayName || "Your profile"} · @{profile.username || "creator"}</p><Link href="/dashboard/settings/profile" className="mt-4 inline-block rounded-lg border border-[var(--border)] px-3 py-2 text-xs font-bold">Edit profile</Link></div><div className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><h2 className="text-lg font-bold">AI Preferences</h2><label className="mt-5 block text-sm font-bold">Preferred tone<select value={tone} onChange={event => setTone(event.target.value)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] p-3"><option>Casual</option><option>Educational</option><option>Funny</option><option>Professional</option><option>Storytelling</option></select></label><label className="mt-5 block text-sm font-bold">Primary goal<select value={goal} onChange={event => setGoal(event.target.value)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] p-3"><option value="">Choose a goal</option><option>Grow my audience</option><option>Get more engagement</option><option>Build a personal brand</option><option>Become a full-time creator</option></select></label><button onClick={save} className="mt-5 rounded-xl bg-[var(--purple)] px-4 py-3 text-sm font-bold">{saved ? "Preferences saved" : "Save preferences"}</button></div><div className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><h2 className="text-lg font-bold">Notification Preferences</h2>{Object.entries(notifications).map(([key, value]) => <label key={key} className="mt-4 flex items-center justify-between gap-4 text-sm text-[var(--muted)]"><span>{key.replace(/[A-Z]/g, letter => ` ${letter}`).replace(/^./, letter => letter.toUpperCase())}</span><input type="checkbox" checked={value} onChange={event => setNotifications({ ...notifications, [key]: event.target.checked })} className="h-4 w-4 accent-[var(--purple)]" /></label>)}<p className="mt-5 text-xs text-[var(--faint)]">Preferences are ready for future notification delivery.</p></div><div className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><h2 className="text-lg font-bold">Account</h2><div className="mt-4 flex flex-wrap gap-3"><LogoutButton /><Link href="/dashboard/billing" className="rounded-xl border border-[var(--border)] px-4 py-2.5 text-sm font-bold">Billing</Link></div><p className="mt-5 text-xs text-[var(--faint)]">Account deletion requires a secure server-side flow and is not enabled until the account-deletion endpoint is configured.</p></div></section></div></main>; }



