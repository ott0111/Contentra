"use client";
import { useState } from "react";
import { useCreator } from "@/components/creator-provider";
export default function ProfileEditor() { const { profile, saveProfile } = useCreator(); const [saved, setSaved] = useState(false); const [bio, setBio] = useState(profile.bio); return <main className="min-h-screen bg-[var(--background)] p-5 sm:p-10"><div className="mx-auto max-w-2xl"><a href="/dashboard" className="text-sm text-[var(--purple-light)]">← Back to dashboard</a><h1 className="mt-10 text-3xl font-bold">Creator profile</h1><p className="mt-2 text-[var(--muted)]">Keep your creator identity ready for every Contentra workflow.</p><section className="mt-8 rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><div className="flex items-center gap-4 border-b border-[var(--border)] pb-6"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-[#2b174d] text-lg font-bold text-[var(--purple-light)]">{profile.avatar || "CR"}</div><div><h2 className="text-xl font-bold">{profile.displayName || "Your name"}</h2><p className="text-sm text-[var(--muted)]">@{profile.username || "username"}</p></div></div><label className="mt-6 block text-sm font-semibold">Bio<textarea value={bio} onChange={event => setBio(event.target.value)} rows={3} className="mt-2 w-full resize-none rounded-xl border border-[var(--border)] bg-[var(--background)] p-4 outline-none focus:border-[var(--purple)]" /></label><div className="mt-6 grid gap-5 sm:grid-cols-2"><Info label="Niche" value={profile.niches.join(" • ")} /><Info label="Target audience" value={profile.targetAudience} /><Info label="Platforms" value={profile.platforms.join(" • ")} /><Info label="Primary goal" value={profile.primaryGoal} /><Info label="Content style" value={profile.contentStyles.join(" • ")} /></div><button onClick={() => { saveProfile({ bio }); setSaved(true); }} className="mt-8 rounded-xl bg-[var(--purple)] px-5 py-3 text-sm font-bold">{saved ? "Profile saved" : "Save profile"}</button></section></div></main> }
function Info({ label, value }: { label: string; value: string }) { return <div><p className="text-xs font-bold uppercase tracking-wider text-[var(--faint)]">{label}</p><p className="mt-2 text-sm text-white">{value || "Not set yet"}</p></div> }