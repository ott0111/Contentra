"use client";
import Link from "next/link";
import { useContent } from "@/components/content-provider";
export default function RecentContent() { const { items } = useContent(); const recent = items.slice(0, 3); return <section className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6"><div className="flex items-center justify-between"><h2 className="font-bold">Recent content</h2><Link href="/dashboard/content" className="text-xs font-bold text-[var(--purple-light)]">View library →</Link></div>{recent.length ? <div className="mt-4 space-y-3">{recent.map(item => <Link key={item.id} href={`/dashboard/content/${item.id}`} className="block rounded-xl border border-[var(--border)] p-3 hover:border-[var(--purple)]"><div className="flex items-center justify-between gap-3"><span className="truncate text-sm font-bold">{item.title || "Untitled content"}</span><span className="shrink-0 text-[10px] font-bold text-[var(--purple-light)]">{item.status}</span></div><p className="mt-1 line-clamp-1 text-xs text-[var(--muted)]">{item.body}</p></Link>)}</div> : <div className="mt-5 rounded-xl border border-dashed border-[var(--border)] p-5 text-sm text-[var(--muted)]">No content yet. <Link href="/dashboard/studio" className="font-bold text-[var(--purple-light)]">Create your first piece →</Link></div>}</section> }
