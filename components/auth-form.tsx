"use client";
import Link from "next/link";
import { useState } from "react";
import { createClient } from "@/lib/supabase/browser";

export function AuthShell({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <main className="flex min-h-screen items-center justify-center bg-[var(--background)] px-5 py-10"><section className="w-full max-w-md"><Link href="/" className="flex items-center gap-3 text-xl font-bold"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[var(--purple)]">C</span>Contentra</Link><div className="mt-12 rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-6 sm:p-8"><h1 className="text-2xl font-bold">{title}</h1><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{description}</p>{children}</div></section></main>; }
function Field({ label, type = "text", value, onChange }: { label: string; type?: string; value: string; onChange: (value: string) => void }) { return <label className="mt-5 block text-sm font-semibold">{label}<input required type={type} value={value} onChange={event => onChange(event.target.value)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-4 py-3 outline-none focus:border-[var(--purple)]" /></label>; }
export function LoginForm() { const supabase = createClient(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setLoading(true); setError(""); const { error: result } = await supabase.auth.signInWithPassword({ email, password }); if (result) setError("Those login details didn't work. Try again."); else window.location.assign("/dashboard"); setLoading(false); }; return <form onSubmit={submit}><Field label="Email" type="email" value={email} onChange={setEmail} /><Field label="Password" type="password" value={password} onChange={setPassword} />{error && <p role="alert" className="mt-4 text-sm text-[var(--purple-light)]">{error}</p>}<button disabled={loading} className="mt-6 w-full rounded-xl bg-[var(--purple)] px-5 py-3 font-bold disabled:opacity-60">{loading ? "Logging in..." : "Log In"}</button><div className="mt-5 flex justify-between text-sm text-[var(--muted)]"><Link href="/forgot-password" className="hover:text-white">Forgot password?</Link><Link href="/signup" className="text-[var(--purple-light)]">Create account</Link></div></form>; }
export function SignupForm() { const supabase = createClient(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); if (password.length < 8) return setError("Use at least 8 characters for your password."); if (password !== confirm) return setError("Passwords do not match."); setLoading(true); setError(""); setMessage(""); try { const { data, error: result } = await supabase.auth.signUp({ email, password, options: { emailRedirectTo: `${window.location.origin}/auth/callback` } }); if (result) setError(result.message); else if (data.session) window.location.assign("/onboarding"); else setMessage("Check your email to confirm your account, then log in."); } catch (caught) { setError(caught instanceof Error ? caught.message : "We couldn't create that account. Try again."); } finally { setLoading(false); } }; return <form onSubmit={submit}><Field label="Email" type="email" value={email} onChange={setEmail} /><Field label="Password" type="password" value={password} onChange={setPassword} /><Field label="Confirm password" type="password" value={confirm} onChange={setConfirm} />{error && <p role="alert" className="mt-4 text-sm text-[var(--purple-light)]">{error}</p>}{message && <p role="status" className="mt-4 text-sm text-[var(--muted)]">{message}</p>}<button disabled={loading} className="mt-6 w-full rounded-xl bg-[var(--purple)] px-5 py-3 font-bold disabled:opacity-60">{loading ? "Creating account..." : "Create Account"}</button><p className="mt-5 text-center text-sm text-[var(--muted)]">Already have an account? <Link href="/login" className="text-[var(--purple-light)]">Log in</Link></p></form>; }
export function ForgotPasswordForm() { const supabase = createClient(); const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const submit = async (event: React.FormEvent) => { event.preventDefault(); const { error: result } = await supabase.auth.resetPasswordForEmail(email, { redirectTo: `${window.location.origin}/reset-password` }); if (result) setError("We couldn't send a reset email right now."); else setMessage("Check your email for a password reset link."); }; return <form onSubmit={submit}><Field label="Email" type="email" value={email} onChange={setEmail} />{error && <p role="alert" className="mt-4 text-sm text-[var(--purple-light)]">{error}</p>}{message && <p role="status" className="mt-4 text-sm text-[var(--muted)]">{message}</p>}<button className="mt-6 w-full rounded-xl bg-[var(--purple)] px-5 py-3 font-bold">Send reset link</button><Link href="/login" className="mt-5 block text-center text-sm text-[var(--purple-light)]">Back to login</Link></form>; }
export function ResetPasswordForm() { const supabase = createClient(); const [password, setPassword] = useState(""); const [confirm, setConfirm] = useState(""); const [error, setError] = useState(""); const submit = async (event: React.FormEvent) => { event.preventDefault(); if (password.length < 8 || password !== confirm) return setError("Use 8 matching characters for your new password."); const { error: result } = await supabase.auth.updateUser({ password }); if (result) setError("We couldn't update your password."); else window.location.assign("/dashboard"); }; return <form onSubmit={submit}><Field label="New password" type="password" value={password} onChange={setPassword} /><Field label="Confirm password" type="password" value={confirm} onChange={setConfirm} />{error && <p role="alert" className="mt-4 text-sm text-[var(--purple-light)]">{error}</p>}<button className="mt-6 w-full rounded-xl bg-[var(--purple)] px-5 py-3 font-bold">Update password</button></form>; }
