const API_URL=import.meta.env.VITE_API_URL??'http://localhost:4000'; export type Workspace={id:string;name:string;type:string};export type Membership={workspaceId:string;role:string;workspace:Workspace};export async function api<T>(path:string,init:RequestInit={},workspaceId?:string):Promise<T>{const headers=new Headers(init.headers);if(init.body)headers.set('content-type','application/json');if(workspaceId)headers.set('x-workspace-id',workspaceId);const r=await fetch(`${API_URL}${path}`,{...init,headers,credentials:'include'});const body=await r.json().catch(()=>null);if(!r.ok)throw new Error(body?.error?.message??'The request could not be completed.');return body.data as T}
