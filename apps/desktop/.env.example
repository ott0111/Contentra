# DESKTOP (TAURI / VITE) — PUBLIC CONFIGURATION
# ----------------------------------------------
# Embedded into the desktop frontend at build time. NEVER place secrets here.

# API base URL the desktop app talks to. Production: https://api.contentra.app
VITE_API_URL=http://localhost:4000