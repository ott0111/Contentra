import { createClient } from "@/lib/supabase/browser";
import type { ContentItem } from "@/types/content";

export function mapContent(row: Record<string, unknown>): ContentItem { return { id: String(row.id), title: String(row.title || ""), body: String(row.body || ""), contentType: String(row.content_type || "General Post"), platform: String(row.platform || "X"), status: row.status as ContentItem["status"], tags: Array.isArray(row.tags) ? row.tags as string[] : [], createdAt: String(row.created_at), updatedAt: String(row.updated_at), scheduledAt: row.scheduled_at ? String(row.scheduled_at) : null }; }
export async function fetchContent() { const { data, error } = await createClient().from("content").select("*").order("created_at", { ascending: false }); if (error) throw error; return (data || []).map(mapContent); }
export async function insertContent(userId: string, item: Omit<ContentItem, "id" | "createdAt" | "updatedAt">) { const { data, error } = await createClient().from("content").insert({ user_id: userId, title: item.title, body: item.body, content_type: item.contentType, platform: item.platform, status: item.status, tags: item.tags, scheduled_at: item.scheduledAt }).select().single(); if (error) throw error; return mapContent(data); }
export async function patchContent(id: string, patch: Partial<ContentItem>) { const update = { ...(patch.title === undefined ? {} : { title: patch.title }), ...(patch.body === undefined ? {} : { body: patch.body }), ...(patch.contentType === undefined ? {} : { content_type: patch.contentType }), ...(patch.platform === undefined ? {} : { platform: patch.platform }), ...(patch.status === undefined ? {} : { status: patch.status }), ...(patch.tags === undefined ? {} : { tags: patch.tags }), ...(patch.scheduledAt === undefined ? {} : { scheduled_at: patch.scheduledAt }), updated_at: new Date().toISOString() }; const { error } = await createClient().from("content").update(update).eq("id", id); if (error) throw error; }
export async function removeContent(id: string) { const { error } = await createClient().from("content").delete().eq("id", id); if (error) throw error; }
